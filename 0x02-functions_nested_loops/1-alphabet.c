#include <stdio.h>
#include <main.h>

/**
 * main - Entry point
 * @0 - Return value
 * Return: Anything
 *
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	       printf(i);
	return (0);
}
